package com.driver;

import org.springframework.stereotype.Repository;

@Repository
public class StudentRepository {

    // Empty class since we don't need any persistent storage for this simple application
}
